; Engine configuration file.
; It's best edited using the editor UI and not directly,
; but it can also be edited via text for minimal setups.

[application]

config/name="Blocks"
config/description="Block primitive library for Godot 4 — composable building blocks with connections, messaging, and visual state."
config/features=PackedStringArray("4.4")

[physics]

common/physics_ticks_per_second=30
